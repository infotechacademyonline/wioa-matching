{
  "name": "wioa-matching",
  "version": "0.1.0",
  "description": "Matches WIOA participants to their nearest office, notifies them by email, and tracks a shared enrollment checklist.",
  "main": "src/server.js",
  "scripts": {
    "migrate": "psql \"$DATABASE_URL\" -f db/schema.sql",
    "geocode": "node src/geocode.js",
    "match": "node src/match.js",
    "notify": "node src/sendNotification.js",
    "start": "node src/server.js"
  },
  "dependencies": {
    "docx": "^9.7.1",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "node-fetch": "^2.7.0",
    "nodemailer": "^9.0.3",
    "pg": "^8.12.0"
  }
}
